/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export',
    basePath: '/live_AirDosa',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
